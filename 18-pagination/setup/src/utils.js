/**Pagination app version 1 - 'App' js file - Features:
 * 
 *      --> Testing the data already piped from 'useFetch'
 *          Custom hook.
 * 
 * Note: The porpouse of utils js is to build and array of
 * arrays.
 * 
 * i already have an array that comes from the API and i will 
 * build and array to set a limit of 'followers' per pagination
 */
const paginate = (followers) => {
    console.log('log from utils js', followers)
}

export default paginate
